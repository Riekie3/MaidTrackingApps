# Local environment config (copy config/database.example.php to config/database.php)
config/database.php

# Real login credentials for local dev accounts — never commit
CREDENTIALS.md

# Uploaded files — never commit real documents/photos
uploads/*/*
!uploads/*/.gitkeep

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# XAMPP / IDE
.vscode/
.idea/
